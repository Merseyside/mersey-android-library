package com.merseyside.archy.presentation.view.calendar.timeUnitView.weekView.recycler

import android.content.Context
import android.util.AttributeSet
import ru.izibook.view.schedule1.timeUnitView.timeUnitRecyclerView.TimeUnitRecyclerView

class WeekRecyclerView(context: Context, attrs: AttributeSet, defStyle: Int) : TimeUnitRecyclerView(
    context, attrs, defStyle
) {

    constructor(context: Context, attrs: AttributeSet): this(context, attrs, 0)

}